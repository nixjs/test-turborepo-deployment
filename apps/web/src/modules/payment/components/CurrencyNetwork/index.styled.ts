import styled from 'styled-components'

export const CurrencyNetworkStyled = styled.div`
    .group-list {
        display: grid;
        grid-template-columns: 3fr 2fr;
        grid-column-gap: 1rem;
        column-gap: 1rem;
        z-index: 11;
        position: relative;
    }
`
