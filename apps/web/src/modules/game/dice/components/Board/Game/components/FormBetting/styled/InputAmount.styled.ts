import { css } from "styled-components"

export const InputAmountStyled = css`
    .input-number {
        &-input {
            padding: 0;
            color: var(--input-number-input-color);
            background-color: transparent;
            border: none;
            outline: none;
            width: 100%;
        }
    }
`
